name: AUR Package Build & Publish

on:
  push:
    branches: ["main"]
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-packages:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: lutris
            packages: lutris-git
          - name: umu-launcher
            packages: umu-launcher-git
          - name: mesa
            packages: mesa-git lib32-mesa-git

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create output directory
        run: mkdir -p dist

      - name: Build ${{ matrix.name }}
        env:
          packages: ${{ matrix.packages }}
        run: |
          for pkg in $packages; do
            ./docker-build-package.sh "$pkg"
          done

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pkg-${{ matrix.name }}
          path: dist/*.pkg.tar.zst
          retention-days: 1

  publish-repo:
    needs: build-packages
    runs-on: ubuntu-latest
    concurrency:
      group: publish-repo
      cancel-in-progress: false
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: new_packages
          pattern: pkg-*
          merge-multiple: true

      - name: Checkout Target Repository
        uses: actions/checkout@v4
        with:
          repository: archlinux-gaming-ci/archlinux-gaming-ci.github.io
          token: ${{ secrets.RELEASE_TOKEN }}
          path: repo-dist
          fetch-depth: 0

      - name: Move Packages and Update Database
        run: |
          mv new_packages/*.pkg.tar.zst repo-dist/x86_64/
          docker run --rm -v ${{ github.workspace }}/repo-dist/x86_64:/repo archlinux/archlinux:base-devel /bin/bash -c "cd /repo && repo-add -n -R archlinux-gaming-ci.db.tar.gz *.pkg.tar.zst"

      - name: Commit and Push (Recreate History)
        working-directory: repo-dist
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git checkout --orphan new_main
          git add -A
          git commit -m "Update packages: $(date -u +'%Y-%m-%d %H:%M:%S')"
          git branch -D main
          git branch -M main
          git push --force origin main
